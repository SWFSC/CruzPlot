% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{state}
\alias{state}
\alias{save_widget}
\alias{update_widget}
\title{CruzPlot load/save state}
\usage{
save_widget(id, type, value = NULL, input = NULL)

update_widget(item, session)
}
\arguments{
\item{id}{The id (character) of the input object, or the name within
the module's \code{\link[shiny:reactiveValues]{shiny::reactiveValues()}}}

\item{type}{the type of the given value. See details for options}

\item{value}{value of the object specified by \code{id}.
If \code{NULL}, then \code{input} must be specified}

\item{input}{R Shiny input object.
The input object passed to the relevant server function.
Ignored if \code{value} is not \code{NULL}. See Details for how object is used}

\item{item}{a list; the output of a \code{save_widget} call}

\item{session}{R Shiny session object.
The session object passed to the relevant server function}
}
\value{
See details
\itemize{
\item \code{save_widget}: a list with three named elements: id, type, and value
\item \code{update_widget}: nothing
}
}
\description{
CruzPlot functions for loading and saving app state
}
\details{
These are helper functions for loading and saving app state.
CruzPlot modules call \code{save_widget} on each value that needs to be saved
to create lists that include the id, type, and value.
These values are saved in the CruzPlot RDATA file.
The value can be passed in explicitly via the \code{value} argument;
if \code{value} is \code{NULL}, then the value will either be
extracted from the \code{input} argument  if \code{input} is not \code{NULL},
or extracted from the output of \code{\link[shiny:domains]{shiny::getDefaultReactiveDomain()}}
if \code{input} is `NULL``.

When the saved state is loaded back into a new CruzPlot session
and passed to the module, the module either
a) updates the appropriate reactive value (custom code within the module)
or b) updates the relevant input using \code{update_widget}.

The accepted \code{type} values, and their corresponding update calls:
\itemize{
\item text: \code{\link[shiny:updateTextInput]{shiny::updateTextInput()}}
\item numeric: \code{\link[shiny:updateNumericInput]{shiny::updateNumericInput()}}
\item select: \code{\link[shiny:updateSelectInput]{shiny::updateSelectInput()}}
\item check: \code{\link[shiny:updateCheckboxInput]{shiny::updateCheckboxInput()}}
\item radio: \code{\link[shiny:updateRadioButtons]{shiny::updateRadioButtons()}}
\item reactive: handled individually by module
}
}
